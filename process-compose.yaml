version: "0.5"

processes:
  nijiiro:
    command: "nix develop -c deno task start"
    working_dir: "~/workspace/nijiiro"
    availability:
      restart: "on_failure"
      max_restarts: 3
  nijiiro:
    command: "./bin/n-high-lovelive"
    working_dir: "~/workspace/n-high-lovelive"
    availability:
      restart: "on_failure"
      max_restarts: 3
